image: nodejscn/node
pages:
  stage: pages
  before_script:
    - npm install -g @angular/cli
    - ng version
    - export NODE_ENV=production
  script:
    - npm install --only=prod
    - ionic build --prod
  artifacts:
    paths:
    - dist
  only:
    - master
